name: Build KomariX frontends
description: Build the vendored KomariX admin frontend and built-in PurCarte theme.

inputs:
  node-version:
    description: Node.js version used to build frontends.
    default: "23"
  artifact-name:
    description: Optional artifact name for the packed frontend bundle.
    default: ""

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}

    - name: Build KomariX admin frontend
      shell: bash
      run: |
        set -euxo pipefail
        rm -rf .build/komari-web
        mkdir -p .build
        cp -a vendor/komari-web .build/komari-web

        # Route Agent install commands to KomariX-owned installers.
        find .build/komari-web/src -type f \( -name '*.ts' -o -name '*.tsx' -o -name '*.js' -o -name '*.jsx' \) -print0 | \
          xargs -0 -r sed -i \
            's#https://raw.githubusercontent.com/komari-monitor/komari-agent/refs/heads/main/#https://raw.githubusercontent.com/kkx999/KomariX/main/agent/#g'

        cd .build/komari-web
        npm install
        npm run build
        cd ../..

        # Visible KomariX branding only; keep application behavior intact.
        find .build/komari-web/dist -type f \( -name '*.js' -o -name '*.html' -o -name '*.json' -o -name '*.css' -o -name '*.webmanifest' \) -print0 | \
          xargs -0 -r sed -i 's/Komari/KomariX/g'

        mkdir -p .build/komari-web/dist/assets
        cp -f docs/komarix.svg .build/komari-web/dist/assets/komarix.svg

        find .build/komari-web/dist -type f \( -name '*.js' -o -name '*.html' -o -name '*.json' -o -name '*.webmanifest' \) -print0 | \
          xargs -0 -r sed -i \
            -e 's#assets/logo\.png#assets/komarix.svg#g' \
            -e 's#/assets/pwa-icon\.png#/assets/komarix.svg#g' \
            -e 's#favicon\.ico#assets/komarix.svg#g'

        rm -rf web/public/defaultTheme
        mkdir -p web/public/defaultTheme/dist
        cp -r .build/komari-web/dist/* web/public/defaultTheme/dist/
        cp -f .build/komari-web/komari-theme.json web/public/defaultTheme/
        sed -i 's/Komari/KomariX/g' web/public/defaultTheme/komari-theme.json
        if [ -f .build/komari-web/preview.png ]; then cp -f .build/komari-web/preview.png web/public/defaultTheme/; fi
        if [ -f .build/komari-web/perview.png ]; then cp -f .build/komari-web/perview.png web/public/defaultTheme/; fi
        if [ -f web/public/defaultTheme/preview.png ] && [ ! -f web/public/defaultTheme/perview.png ]; then cp -f web/public/defaultTheme/preview.png web/public/defaultTheme/perview.png; fi
        if [ -f web/public/defaultTheme/perview.png ] && [ ! -f web/public/defaultTheme/preview.png ]; then cp -f web/public/defaultTheme/perview.png web/public/defaultTheme/preview.png; fi

    - name: Build built-in KomariX PurCarte theme
      shell: bash
      run: |
        set -euxo pipefail
        rm -rf .build/purcarte
        mkdir -p .build
        cp -a vendor/purcarte .build/purcarte

        mkdir -p .build/purcarte/public/assets
        cp -f docs/komarix.svg .build/purcarte/public/assets/komarix.svg

        # Use KomariX logo/title in the built-in copy.
        find .build/purcarte/src -type f \( -name '*.ts' -o -name '*.tsx' -o -name '*.js' -o -name '*.jsx' \) -print0 | \
          xargs -0 -r sed -i \
            -e 's#/assets/logo\.png#/assets/komarix.svg#g' \
            -e 's/Komari/KomariX/g'

        cd .build/purcarte
        npm install
        npm run build
        cd ../..

        find .build/purcarte/dist -type f \( -name '*.js' -o -name '*.html' -o -name '*.json' -o -name '*.css' -o -name '*.webmanifest' \) -print0 | \
          xargs -0 -r sed -i 's/Komari/KomariX/g'

        rm -rf web/public/purcarteTheme
        mkdir -p web/public/purcarteTheme/dist
        cp -r .build/purcarte/dist/* web/public/purcarteTheme/dist/
        cp -f .build/purcarte/komari-theme.json web/public/purcarteTheme/
        if [ -f .build/purcarte/preview.png ]; then cp -f .build/purcarte/preview.png web/public/purcarteTheme/; fi

    - name: Pack frontend artifact
      if: ${{ inputs.artifact-name != '' }}
      shell: bash
      run: |
        set -euxo pipefail
        tar -czf "$RUNNER_TEMP/${{ inputs.artifact-name }}.tar.gz" \
          -C web/public defaultTheme purcarteTheme

    - name: Upload frontend artifact
      if: ${{ inputs.artifact-name != '' }}
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.artifact-name }}
        path: ${{ runner.temp }}/${{ inputs.artifact-name }}.tar.gz
